#include <iostream>
#include <cmath>
using namespace std;

int height(float distance,float angle);

main()
{
	float h,d,x,y;
	cout << "Enter the distance from the base of the tree (in feet): ";
	cin >> d;
	cout << "Enter the angle of elevation (in degrees): ";
	cin >> x;
	y = height()
}

int height(float distance,float angle)
{
	angle = x * 57.2958;
	return angle;
}